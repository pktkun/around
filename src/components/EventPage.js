import React, { FC } from 'react';
import ThreadList from "src/components/ThreadList";

const EventPage = () => {
  return (
    <div>
      <ThreadList />
    </div>
  );
};

export default EventPage;